name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [24]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/checkout@v7
        with:
          repository: Reldnahc/tcgplayer-private-api
          ref: 5bd5dddef6754fe264edaaa3e96c1ad200099f86
          path: api-package
      - uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm
      - name: Pack API dependency
        run: |
          npm ci --prefix api-package
          mkdir -p .packages
          cd api-package
          npm pack --pack-destination ../.packages
      - run: npm ci
      - run: npm run check
      - name: Install browser test runtime
        if: matrix.node-version == 24
        run: npx playwright install --with-deps chromium
      - name: Verify operator UI in Chromium
        if: matrix.node-version == 24
        run: npm run test:browser
      - run: npm run audit
